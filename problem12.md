# Problem 12: Highly Divisible Triangular Number

**Kaynak:** https://projecteuler.net/problem=12

## Problem Metni (orijinal, İngilizce)

> The sequence of triangle numbers is generated by adding the natural numbers. So the 7th triangle number would be 1 + 2 + 3 + 4 + 5 + 6 + 7 = 28. The first ten terms would be:
>
> 1, 3, 6, 10, 15, 21, 28, 36, 45, 55, ...
>
> Let us list the factors of the first seven triangle numbers:
>
> 1: 1
> 3: 1,3
> 6: 1,2,3,6
> 10: 1,2,5,10
> 15: 1,3,5,15
> 21: 1,3,7,21
> 28: 1,2,4,7,14,28
>
> We can see that 28 is the first triangle number to have over five divisors.
>
> What is the value of the first triangle number to have over five hundred divisors?

## Problem Metni (Türkçe Çeviri)

> Üçgen sayılar dizisi, doğal sayılar birbirine eklenerek oluşturulur. Buna göre 7. üçgen sayı 1 + 2 + 3 + 4 + 5 + 6 + 7 = 28 olur. İlk on terim şöyledir:
>
> 1, 3, 6, 10, 15, 21, 28, 36, 45, 55, ...
>
> İlk yedi üçgen sayının bölenlerini listeleyelim:
>
> 1: 1
> 3: 1,3
> 6: 1,2,3,6
> 10: 1,2,5,10
> 15: 1,3,5,15
> 21: 1,3,7,21
> 28: 1,2,4,7,14,28
>
> Görüldüğü gibi 28, beşten fazla bölene sahip ilk üçgen sayıdır.
>
> Beş yüzden fazla bölene sahip ilk üçgen sayının değeri nedir?

## Kullanılan Algoritma

Kod, üçgen sayıları doğrudan asal çarpanlarına ayırmak yerine matematiksel bir kısayoldan yararlanan bir arama algoritması kullanır.

- `countDivisors(long long n)` fonksiyonu, `n` sayısını asal çarpanlarına ayırarak (deneme bölmesi ile `p = 2`'den `p * p <= n` olduğu sürece) bölen sayısını hesaplar: her asal çarpan `p` için üssü (`exponent`) sayılır ve bölen sayısı formülü `count *= (exponent + 1)` şeklinde çarpımsal olarak biriktirilir (bir sayı `p1^e1 * p2^e2 * ...` şeklinde çarpanlanıyorsa toplam bölen sayısı `(e1+1)*(e2+1)*...`'dir). Döngü bittiğinde geriye `n > 1` büyüklüğünde bir asal çarpan kalmışsa (yani `n`'in `sqrt(n)`'den büyük tek bir asal çarpanı varsa), `count *= 2` ile bu da hesaba katılır.
- `main()` içinde üçgen sayılar, klasik toplama yerine kapalı formülle `triangle = n(n+1)/2` şeklinde üretilir. Buradaki kilit optimizasyon şudur: `n` ve `n+1` ardışık iki tam sayı olduğundan aralarında ortak bölen yoktur (coprime); bu nedenle `n(n+1)/2` sayısının bölen sayısı, `n` ve `n+1`'i (biri 2'ye bölünmüş haliyle) ayrı ayrı çarpanlarına ayırıp bölen sayılarını çarparak da bulunabilir (bölen sayısı fonksiyonu çarpımsaldır). Kodda `a = n`, `b = n + 1` alınır; ikisinden çift olanı `2`'ye bölünerek (`a % 2 == 0` ise `a/2`, değilse `b/2`) coprime iki parçaya ayrılır ve `divisors = countDivisors(a/2) * countDivisors(b)` ya da `divisors = countDivisors(a) * countDivisors(b/2)` şeklinde iki ayrı (ve daha küçük) sayının bölen sayıları çarpılarak toplam bölen sayısı bulunur. Bu, doğrudan büyüyen `triangle` değerini çarpanlarına ayırmaktan çok daha hızlıdır.
- `n = 1`'den başlanarak her adımda bu şekilde hesaplanan `divisors` değeri `500`'ü geçene kadar `n` bir artırılır (`n++`); `divisors > 500` koşulu sağlandığı anda o adımdaki `triangle` değeri (yani `n(n+1)/2`) ilk 500'den fazla bölene sahip üçgen sayı olarak yazdırılır.

Yaklaşımın özeti: brute-force artan `n` taraması ile, her adımda üçgen sayının bölen sayısını coprime çarpanlara bölerek hızlı hesaplayan bir asal çarpanlara ayırma (prime factorization) tabanlı algoritmadır.

## Çözüm Dosyası

`problem12.c`
